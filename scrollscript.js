window.addEventListener('scroll', function () {
    // Логика обработки прокрутки
    if (window.scrollY > 0) {
        console.log('Scrolling down');
        // можно выполнить что-то специфическое при прокрутке
    }
});
